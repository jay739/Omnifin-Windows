<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OmnifinNative.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmnifinNative.Views"
    xmlns:models="using:OmnifinNative.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="0,0,0,24">
            <FontIcon Glyph="&#xEA8A;" FontSize="28" Foreground="{ThemeResource SystemAccentColor}" VerticalAlignment="Center" />
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
            <ProgressRing x:Name="LoadingRing" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="8,0,0,0" Visibility="Collapsed" />
        </StackPanel>

        <!-- Main Scrollable Dashboard Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Spacing="24">
                
                <!-- 4-Column Metrics Grid -->
                <Grid ColumnSpacing="16" RowSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- User Statistics Card -->
                    <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="36" Height="36" CornerRadius="18" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE77B;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="User Accounts" FontSize="14" FontWeight="Medium" Opacity="0.7" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{x:Bind TotalUsersText, Mode=OneWay}" FontSize="32" FontWeight="Bold" />
                            <TextBlock Text="{x:Bind ActiveUsersText, Mode=OneWay}" FontSize="12" Opacity="0.6" />
                            <ProgressBar Maximum="{x:Bind TotalUsersCount, Mode=OneWay}" Value="{x:Bind ActiveUsersCount, Mode=OneWay}" Foreground="{ThemeResource SystemAccentColor}" Height="4" CornerRadius="2" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>

                    <!-- Server Watch Time Card -->
                    <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="36" Height="36" CornerRadius="18" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE768;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="Watch Time" FontSize="14" FontWeight="Medium" Opacity="0.7" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{x:Bind WatchTimeText, Mode=OneWay}" FontSize="32" FontWeight="Bold" />
                            <TextBlock Text="{x:Bind StreamersCountText, Mode=OneWay}" FontSize="12" Opacity="0.6" />
                        </StackPanel>
                    </Border>

                    <!-- Outstanding Invites Card -->
                    <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="36" Height="36" CornerRadius="18" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE71B;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="Invite Codes" FontSize="14" FontWeight="Medium" Opacity="0.7" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="{x:Bind InvitesCountText, Mode=OneWay}" FontSize="32" FontWeight="Bold" />
                            <TextBlock Text="{x:Bind InvitesUsesText, Mode=OneWay}" FontSize="12" Opacity="0.6" />
                        </StackPanel>
                    </Border>

                    <!-- Integration Health Card -->
                    <Border Grid.Column="3" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="36" Height="36" CornerRadius="18" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE95A;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="Integrations" FontSize="14" FontWeight="Medium" Opacity="0.7" VerticalAlignment="Center" />
                            </StackPanel>
                            <Grid RowSpacing="8" ColumnSpacing="12" Margin="0,4,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                    <Ellipse Width="10" Height="10" Fill="{x:Bind SmtpBrush, Mode=OneWay}" VerticalAlignment="Center" />
                                    <TextBlock Text="SMTP Mail" FontSize="12" Opacity="0.7" VerticalAlignment="Center" />
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                    <Ellipse Width="10" Height="10" Fill="{x:Bind TgBrush, Mode=OneWay}" VerticalAlignment="Center" />
                                    <TextBlock Text="Telegram" FontSize="12" Opacity="0.7" VerticalAlignment="Center" />
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                    <Ellipse Width="10" Height="10" Fill="{x:Bind DiscordBrush, Mode=OneWay}" VerticalAlignment="Center" />
                                    <TextBlock Text="Discord" FontSize="12" Opacity="0.7" VerticalAlignment="Center" />
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                    <Ellipse Width="10" Height="10" Fill="{x:Bind MatrixBrush, Mode=OneWay}" VerticalAlignment="Center" />
                                    <TextBlock Text="Matrix" FontSize="12" Opacity="0.7" VerticalAlignment="Center" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Quick Administration Panel Section -->
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,8,0,0">
                    <FontIcon Glyph="&#xE7AC;" FontSize="20" Foreground="{ThemeResource SystemAccentColor}" VerticalAlignment="Center" />
                    <TextBlock Text="Quick Actions" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
                
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Backup Action Card -->
                    <Button x:Name="BackupButton" Grid.Column="0" Click="BackupButton_Click"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Padding="20" CornerRadius="8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="32" Height="32" CornerRadius="16" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE8C8;" FontSize="14" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="Backup Database" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="Create a snapshot of the backend database and config on the server." FontSize="11" Opacity="0.5" TextWrapping="Wrap" />
                        </StackPanel>
                    </Button>

                    <!-- Housekeeping Action Card -->
                    <Button x:Name="HousekeepingButton" Grid.Column="1" Click="HousekeepingButton_Click"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Padding="20" CornerRadius="8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="32" Height="32" CornerRadius="16" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="Housekeeping" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="Clean expired accounts, old invites, and clear activity logs." FontSize="11" Opacity="0.5" TextWrapping="Wrap" />
                        </StackPanel>
                    </Button>

                    <!-- Restart Action Card -->
                    <Button x:Name="RestartButton" Grid.Column="2" Click="RestartButton_Click"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Padding="20" CornerRadius="8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Width="32" Height="32" CornerRadius="16" Background="{ThemeResource SystemAccentColor}" Opacity="0.15">
                                    <FontIcon Glyph="&#xE777;" FontSize="14" Foreground="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="Restart Server" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Text="Gracefully reboot the backend server. Active sessions will be logged out." FontSize="11" Opacity="0.5" TextWrapping="Wrap" />
                        </StackPanel>
                    </Button>
                </Grid>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
