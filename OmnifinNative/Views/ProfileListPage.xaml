<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OmnifinNative.Views.ProfileListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmnifinNative.Views"
    xmlns:models="using:OmnifinNative.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="0,0,0,8">
            <FontIcon Glyph="&#xEE56;" FontSize="24" Foreground="{ThemeResource SystemAccentColor}" VerticalAlignment="Center" />
            <TextBlock Text="Profiles" Style="{StaticResource PageHeaderStyle}" />
            <ProgressRing x:Name="LoadingRing" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="8,0,0,0" Visibility="Collapsed" />
        </StackPanel>

        <!-- Command Bar -->
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" HorizontalAlignment="Left" Background="Transparent" IsOpen="False" Margin="0,0,0,8">
            <AppBarButton Icon="Refresh" Label="Refresh" Click="RefreshButton_Click" />
            <AppBarButton Label="Create Profile">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE710;" />
                </AppBarButton.Icon>
                <AppBarButton.Flyout>
                    <Flyout x:Name="CreateFlyout" Opened="CreateFlyout_Opened">
                        <StackPanel Width="280" Spacing="10" Padding="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE710;" FontSize="18" Foreground="{ThemeResource SystemAccentColor}" />
                                <TextBlock Text="Create New Profile" FontSize="16" FontWeight="SemiBold" />
                            </StackPanel>
                            
                            <TextBox x:Name="ProfileNameBox" Header="Profile Name" PlaceholderText="e.g. FriendsProfile" />
                            
                            <ComboBox x:Name="SourceUserComboBox" Header="Sourced From User" HorizontalAlignment="Stretch" DisplayMemberPath="Name" />
                            
                            <CheckBox x:Name="HomescreenCheckBox" Content="Store Homescreen Layout" IsChecked="True" />
                            
                            <CheckBox x:Name="JellyseerrCheckBox" Content="Generate Jellyseerr Profile" IsChecked="True" />

                            <Button x:Name="ConfirmCreateButton" Content="Create" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Click="ConfirmCreateButton_Click" Margin="0,8,0,0" />
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton Label="Set Default" Click="SetDefaultButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE735;" />
                </AppBarButton.Icon>
            </AppBarButton>

            <AppBarSeparator />

            <AppBarButton Label="Delete" Click="DeleteButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE74D;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <ListView
            x:Name="ProfilesListView"
            Grid.Row="2"
            BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            BorderThickness="1"
            CornerRadius="4"
            ItemsSource="{x:Bind Profiles}"
            SelectionMode="Single">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,6" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Profile Name" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="Source User" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="2" Text="Admin" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="3" Text="Jellyseerr" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="4" Text="Ombi" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="5" Text="Referrals" FontWeight="SemiBold" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ProfileInfo">
                    <Grid Padding="12,6" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind DisplayName}" FontWeight="Medium" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{x:Bind FromUser}" VerticalAlignment="Center" Opacity="0.8" />
                        <TextBlock Grid.Column="2" Text="{x:Bind AdminDisplay}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="3" Text="{x:Bind JellyseerrDisplay}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="4" Text="{x:Bind OmbiDisplay}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="5" Text="{x:Bind ReferralsDisplay}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
