<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OmnifinNative.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmnifinNative"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="320"
            Spacing="12">

            <TextBlock Text="Omnifin" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" />
            <TextBlock Text="Sign in with your admin account" Opacity="0.7" HorizontalAlignment="Center" Margin="0,0,0,12" />

            <TextBox
                x:Name="ServerUrlBox"
                Header="Server URL"
                PlaceholderText="https://your-omnifin-server.example.com" />

            <TextBox x:Name="UsernameBox" Header="Username" />
            <PasswordBox x:Name="PasswordBox" Header="Password" />

            <TextBlock
                x:Name="ErrorText"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />

            <Button
                x:Name="LoginButton"
                Content="Sign in"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Style="{StaticResource AccentButtonStyle}"
                Click="LoginButton_Click"
                Margin="0,8,0,0" />

            <ProgressRing x:Name="LoginProgress" IsActive="False" Width="24" Height="24" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
