<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OmnifinNative.Views.BackupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OmnifinNative.Views"
    xmlns:models="using:OmnifinNative.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="0,0,0,8">
            <FontIcon Glyph="&#xE8C8;" FontSize="24" Foreground="{ThemeResource SystemAccentColor}" VerticalAlignment="Center" />
            <TextBlock Text="Backups" Style="{StaticResource PageHeaderStyle}" />
            <ProgressRing x:Name="LoadingRing" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="8,0,0,0" Visibility="Collapsed" />
        </StackPanel>

        <!-- Command Bar -->
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" HorizontalAlignment="Left" Background="Transparent" IsOpen="False" Margin="0,0,0,8">
            <AppBarButton Icon="Refresh" Label="Refresh" Click="RefreshButton_Click" />
            <AppBarButton Label="Create Backup" Click="CreateBackupButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE710;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="RestoreButton" Label="Restore Selected" Style="{StaticResource AccentButtonStyle}" Click="RestoreButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE895;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <!-- Backup List -->
        <ListView
            x:Name="BackupsListView"
            Grid.Row="2"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            ItemsSource="{x:Bind Backups}"
            SelectionMode="Single">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="16,10" ColumnSpacing="16" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Backup Name" FontWeight="SemiBold" FontSize="12" Opacity="0.7" />
                        <TextBlock Grid.Column="1" Text="Creation Date" FontWeight="SemiBold" FontSize="12" Opacity="0.7" />
                        <TextBlock Grid.Column="2" Text="Size" FontWeight="SemiBold" FontSize="12" Opacity="0.7" />
                        <TextBlock Grid.Column="3" Text="Commit Version" FontWeight="SemiBold" FontSize="12" Opacity="0.7" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:BackupInfo">
                    <Grid Padding="16,8" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE8C8;" FontSize="14" Foreground="{ThemeResource SystemAccentColor}" />
                            <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" FontWeight="Medium" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{x:Bind FormattedDate}" VerticalAlignment="Center" FontSize="13" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Size}" VerticalAlignment="Center" FontSize="13" />
                        <TextBlock Grid.Column="3" Text="{x:Bind Commit}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" FontSize="13" Opacity="0.6" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty State -->
        <StackPanel
            x:Name="EmptyState"
            Grid.Row="2"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Spacing="8"
            Visibility="Collapsed">
            <FontIcon Glyph="&#xE8C8;" Style="{StaticResource EmptyStateIconStyle}" />
            <TextBlock Text="No backups found" Style="{StaticResource EmptyStateTitleStyle}" />
            <TextBlock Text="Create a snapshot backup of your SQLite database file" Style="{StaticResource EmptyStateSubtitleStyle}" />
        </StackPanel>
    </Grid>
</Page>
